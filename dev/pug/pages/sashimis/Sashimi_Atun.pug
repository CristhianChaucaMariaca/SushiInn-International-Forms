extends ../../templates/template-plate.pug
include ../../mixins/plato
append config
    //- TODO: * Modificar el numero de plato. Y el nombre del objeto
    - plato = 3;
    //- - numSponsor= 0; 
    - page = sashimis.content[plato].text
    - metadescripcion = sashimis.content[plato].description
block content
    // TODO: 
    +plato(sashimis.content[plato], sponsor[numSponsor],sashimis.category)
            
    include ../../blocks/btn_atras
    //- TODO:
    +btn(sashimis.category)