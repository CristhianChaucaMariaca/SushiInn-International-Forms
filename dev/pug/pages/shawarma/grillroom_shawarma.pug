extends ../../templates/template-plate.pug
include ../../mixins/plato
append config
    //- TODO: * Modificar el numero de plato. Y el nombre del objeto
    - plato = 2;
    //- - numSponsor= 0; 
    - page = shawarma.content[plato].text
    - metadescripcion = shawarma.content[plato].description
block content
    // TODO: 
    +plato(shawarma.content[plato], sponsor[numSponsor],shawarma.category)
            
    include ../../blocks/btn_atras
    //- TODO:
    +btn(shawarma.category)